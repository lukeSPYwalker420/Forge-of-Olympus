{
  "name": "forge-of-olympus-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "generate-programs": "node scripts/generatePrograms.js",
    "dev": "npm run generate-programs && cd frontend && npm run dev",
    "build": "npm run generate-programs && cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.5",
    "stripe": "^17.0.0"
  }
}